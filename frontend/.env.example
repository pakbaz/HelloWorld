# Repository mode: "http" uses the local backend, "pglite" uses in-browser PGlite for static/GitHub Pages
VITE_REPOSITORY_MODE=http

# Backend API base URL (only used when VITE_REPOSITORY_MODE=http)
VITE_API_BASE_URL=http://localhost:8000/api
