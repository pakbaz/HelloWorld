# Repository mode: "http" (local dev backend) or "pglite" (static/GitHub Pages)
VITE_REPOSITORY_MODE=http

# Backend API base URL (only used when VITE_REPOSITORY_MODE=http)
VITE_API_BASE_URL=http://localhost:8000/api
